/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'unsplash.com',
      'images.unsplash.com',
      'source.unsplash.com',
      'storage.googleapis.com',
    ],
  },
};

module.exports = nextConfig;
